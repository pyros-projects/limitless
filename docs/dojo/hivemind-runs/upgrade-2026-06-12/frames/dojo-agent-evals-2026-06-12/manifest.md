# Manifest — dojo-agent-evals — 2026-06-12 (edition 2 run)
mode: trend scan · window 7d · venues r/AI_Agents, r/LocalLLaMA, X
queries: q1-q6 per config
results: topic 1 — 14 relevant items (SWE-bench multimodal thread 412↑;
two new leaderboard launches); topic 2 — 9 relevant items (tracing-infra
debate 230↑); topic 3 — 0 relevant items.
adaptations: topic 3 ("formal verification for agent behavior") thin for
the THIRD consecutive sweep — 0 relevant hits after floor halving AND
window widening to 14d. Staleness threshold (2 consecutive, met since
2026-06-02) → replacement proposal issued with this run's brief,
pending user confirmation. No config edit performed. No other
adaptations.
diff vs 2026-06-02: previous brief is excerpt-level (no itemized
stories), so the diff ran against the named stories of both prior
frames (SWE-bench Verified update, LangSmith eval threads). All of this
run's items are new; nothing dropped as already-reported.
rejections: none named at hand-off — relevant-item counts were received
post-triage in this continuation; no rejection list was available to
record.
note: raw/ fan-out JSON was not persisted in this continuation —
re-triage from this frame is limited to the counts and stories named
above.
